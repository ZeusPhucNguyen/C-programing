#include <stdio.h>
#include <conio.h>
void main ()
{
	int a,b ;
	printf("Nhap so ban muon tao bang cuu chuong:",a);
	scanf("%d",&a);
	for (b=1;b <=10; b ++)
	printf(" %d * %d = %d \n",a,b,a*b);
}

